# 本地开发环境变量
# 运行 wrangler dev 前需要配置 KV Namespace
# 使用 wrangler kv:namespace create SUBSCRIPTIONS 创建

# ------------------------------------------
# Cloudflare Queues 配置
# ------------------------------------------
# 在本地开发时，Cloudflare Queues 需要通过 Wrangler 模拟
# 使用 wrangler dev --local 时，Queues 会自动模拟

# ------------------------------------------
# Cloudflare R2 存储配置
# ------------------------------------------
# 在本地开发时，R2 需要通过 Wrangler 模拟
# R2 绑定已在 wrangler.toml 中配置：BACKUP_BUCKET

# ------------------------------------------
# 数据库配置
# ------------------------------------------
# D1 数据库需要先创建并绑定：
# 1. npx wrangler d1 create bee-swarm-db
# 2. 将返回的 database_id 填入 wrangler.toml 的 D1_DATABASE_ID

# ------------------------------------------
# 其他配置
# ------------------------------------------
# ADMIN_PASSWORD - 管理员密码（生产环境使用 wrangler secret put）
# ADMIN_EMAIL - 指定管理员邮箱（可选）
# ALLOWED_ORIGINS - 允许的跨域来源（逗号分隔）
# LOG_LEVEL - 日志级别（debug|info|warn|error）
